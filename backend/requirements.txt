# ThinkSync Backend — Python dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Data validation
pydantic[email]>=2.7.0
pydantic-settings>=2.2.0

# SSH
asyncssh>=2.14.2

# Database (Supabase)
supabase>=2.4.0

# Authentication
PyJWT>=2.8.0
cryptography>=42.0.0

# Form / multipart support (required by FastAPI for file uploads, future use)
python-multipart>=0.0.9

# LLM provider
openai>=1.40.0

# Redis client (optional — used by Forge v2 for LLM response caching)
redis>=5.0.0

# HTTP client (used by run_agent to call /commands/execute internally)
httpx>=0.27.0
